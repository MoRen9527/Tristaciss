apiVersion: v1
kind: Service
metadata:
  name: api-server
  namespace: api-serverSPACE
spec:
  selector:
    app: api-server
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP

